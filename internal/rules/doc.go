// Package rules implements a tiny boolean expression language (matchers)
// used to select Traefik dynamic configuration objects. It provides a lexer,
// parser that builds a small AST, and an evaluator that tests a context.
package rules
